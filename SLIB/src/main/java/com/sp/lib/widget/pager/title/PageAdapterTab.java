package com.sp.lib.widget.pager.title;

import android.content.Context;
import android.util.AttributeSet;

/**
 * according to adapter
 */
public class PageAdapterTab extends TextPageTab{
    public PageAdapterTab(Context context) {
        super(context);
    }

    public PageAdapterTab(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public PageAdapterTab(Context context, AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);
    }
}
